import { Cpu, FlaskConical, Briefcase, Scale, HeartPulse, type LucideIcon } from "lucide-react"

export const ICON_MAP: Record<string, LucideIcon> = {
  Cpu,
  FlaskConical,
  Briefcase,
  Scale,
  HeartPulse,
}
