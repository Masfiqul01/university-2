Place two site assets here:

- logokacst.webp  -> used as site logo (lib/site-assets.ts LOGO_URL)
- benner1.webp    -> used as homepage hero banner (mapped from /images/hero-campus.png)

Add the actual .webp files to this folder. Filenames must match exactly.
